export default function Dashboard({ session }) {
    return (
        <div style={{ padding: 40 }}>
            <h2>Dashboard</h2>
            <p>Welcome back. Your goals and activity summary will live here.</p>
        </div>
    )
}